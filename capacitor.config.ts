import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nimashi.cakeshop',
  appName: 'cakeshop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
